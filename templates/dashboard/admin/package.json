{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "build:ssr": "tsx build.ts --ssr",
    "dev": "tsx build.ts --watch",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "@getforma/core": "^1.0.0"
  },
  "devDependencies": {
    "@getforma/build": "^0.1.0",
    "@getforma/compiler": "^0.1.0",
    "@playwright/test": "^1.58.2",
    "@tailwindcss/cli": "^4.1.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
