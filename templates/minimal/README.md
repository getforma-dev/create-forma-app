# {{PROJECT_NAME}}

Built with [Forma](https://getforma.dev).

## Development

```bash
# Build frontend
cd admin && npm install && npm run build && cd ..

# Run server
cargo run
# Open http://127.0.0.1:3000
```
